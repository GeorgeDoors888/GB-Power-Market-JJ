.venv/
__pycache__/
*.pyc
.env
node_modules/
dist/
build/
tools/ai_memory/indexdir/
context/
.DS_Store
**/.DS_Store
credentials.json
oauth_credentials.json
token.json

# IRIS credentials - NEVER commit!
iris_settings.json
iris_data/
# Note: .vscode/settings.json is allowed for workspace config

# Gemini API key - NEVER commit!
gemini_api_key.txt

# Google Maps API key - NEVER commit!
GOOGLE_MAPS_API_KEY.md

# Smart Grid service account credentials - NEVER commit!
smart_grid_credentials.json
inner-cinema-*.json
bigquery_credentials.json
**/bigquery_credentials.json

# Sensitive credentials - DO NOT COMMIT
smart_grid_credentials.json
credentials.json
PROJECT_CREDENTIALS_CONFIG.json
**/*credentials*.json
/Users/georgemajor/GB Power Market JJ && ls -1 *.json 2>/dev/null | grep -i credentialtemplate*.json
token.pickle
*.pickle

# Exported documentation (generate locally, don't commit)
API_OVERVIEW.pdf
API_OVERVIEW_*.pdf
API_OVERVIEW.html
API_OVERVIEW.docx
API_OVERVIEW.txt
github-deploy-key.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Chat history - Keep in git for knowledge preservation
# Uncomment below to make chat history local-only
# chat-history/*.md

# Service Account Keys (SENSITIVE)
*service_account*.json

# Additional credentials - NEVER commit!
arbitrage-bq-key.json
.env.ai-gateway
api-gateway-deploy.tar.gz
deploy_package/

# Large directories causing slowness
iris_windows_deployment/
overarch-jibber-jabber/
logs/*.log
*.html
dno_api_exploration_*.json
offshore_wind_farms_backup_*.csv

# Output directories
output/
*.png
*.csv
duos_parsing_log.txt

# Backup files
*_backup_*.csv
*_backup_*.json

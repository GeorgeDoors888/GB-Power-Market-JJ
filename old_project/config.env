# Configuration for Elexon & NESO data collection system
# This file contains settings for the data downloaders and loaders

# Google Cloud settings
PROJECT_ID=jibber-jabber-knowledge
BUCKET_NAME=jibber-jabber-knowledge-bmrs-data
DATASET_ID=uk_energy_prod
REGION=europe-west2

# Date ranges for data collection
START_DATE=2016-01-01
END_DATE=$(date +"%Y-%m-%d")

# API settings
# Note: API keys should be stored in api.env, not here
API_BASE_URL=https://data.elexon.co.uk/bmrs/api/v1

# Execution settings
MAX_DAYS_PER_REQUEST=7
MAX_WORKERS=4
RETRY_ATTEMPTS=5
BACKOFF_FACTOR=0.6
TIMEOUT=30

# Priority settings
# Use these to control which datasets are downloaded first
# Valid values: very-high, high, medium, low
DEFAULT_PRIORITY=high
